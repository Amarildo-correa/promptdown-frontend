openapi: 3.0.0

info:
    title: Promptdown Contracts API
    description: API de contratos para Promptdown
    version: 1.0.0
    x-specification-reference: https://spec.openapis.org/oas/v3.0.3

# NOTA: Esta especificação deverá ser documentada e expandida no futuro com:
#
# Seções a criar:
# - servers: Definir URLs dos ambientes (Production, Staging, Development)
# - paths: Endpoints completos para CRUD de contratos (/contracts, /contracts/{id})
# - components:
#   - schemas: Modelos de dados (Contract, CreateContractRequest, UpdateContractRequest, Error, etc)
#   - responses: Respostas padrão (200, 400, 401, 404, 422, 500)
#   - securitySchemes: Autenticação via JWT Bearer Token
# - security: Aplicar autenticação nos endpoints
# - tags: Categorizar endpoints por funcionalidade
#
# Detalhes de implementação:
# - Autenticação: Bearer Token JWT obrigatório em todos os endpoints
# - Paginação: Implementar para listagem de contratos
# - Validação: Esquemas JSON Schema com validação de entrada
# - Soft Delete: Suportar exclusão lógica de contratos
# - Metadados: Campos customizados para extensibilidade

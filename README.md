MUST​ Pode escrever os arquivos tocados pelo seu plano.

## Adendo sobre o projeto:

### Especificidade do repositório promptdown-frontend​ para o projeto Promptdown.

Repositório promptdown-frontend é um repositório multirepo para frontend, contendo contrato de API real (openapi.yaml​) e servidor JSON Server "servidor HTTP para testar repositório enquanto backend (outro repositório multirepo) não é desenvolvido".
